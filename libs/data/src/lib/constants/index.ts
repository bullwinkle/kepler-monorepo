export enum ControlType {
  INPUT = "input",
  NUMERIC = "numeric",
  MULTI = "multi-choice",
  SINGLE = "single-choice",
}

export enum SSEType {
  NOTICE = "notice",
  UPDATE = "update",
  MESSAGE = "message",
}
